services:
  redis:
    image: redis:7-alpine
  worker:
    build:
      context: .
      dockerfile: docker/worker/Dockerfile
  web:
    build:
      context: .
      dockerfile: docker/web/Dockerfile
    ports: 
      - "8000:8000" 
